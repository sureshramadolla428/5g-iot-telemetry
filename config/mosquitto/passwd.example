# Template only. Runtime password file is generated by the mosquitto-init
# container from MQTT_USERNAME / MQTT_PASSWORD in .env (never commit real hashes).
#
# Manual equivalent:
#   mosquitto_passwd -b -c passwd iotuser 'your-password'
iotuser:$7$placeholder_not_used_at_runtime
